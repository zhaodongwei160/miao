  # JAVRSCRIPT   
CSS是心智模型
以前js只能用于网页里的编程语言，现在可以用于：
    * Node，js
    * 微信小程序  
    * harmony OS     
    * MongoDB （数据库Access
    * Auto，js（安卓系一个自动化操作软件）
* 编程语言说到底就三种结构：
        顺序结构
        选择结构
        循环结构
     如果我们说一个编程语言是一个图灵完全（turning complate）的，那意味着它可以表达一个完整逻辑。

     CodeMirror javascript作者写的
#   6月7日课程 chapter1
    JS基本类型
#   数值nunmbers，字符号stings，布尔/真假Booleans，对象，函数，未定义的
    js最多表示2^64比特，大约1*10^（-）308，显示为无穷大 infinity 负无穷大-infinity
    2^53的整数能保证运算精确
    %（模） 余数； 5/2的%为1
*   NaN （not a number）无意义的结果
#   strings 字符串 ""
    字符串相加,叫做拼接运算，比如 "12"+"11" = 1211
*   escaping 转义 \n newline \t tab
* + 字符串拼接 concetenate
#   一元运算符 给一个字符串类型命名，console.logo(typeof 2.5)   console.log( 3*100 )  输出到控制台
#   console.log()：可以将变量输出到浏览器的控制台中，方便开发者调用JS代码，它是一个使用频率很高的功能。
*  二元运算符 1+1，1*1等等
*  减号可以同时作为一元和二元运算  -5， 5-3；
#   布尔类型的值 Boolean values
    代表ture/false
*  比较运算符 conparsions 
    3>2, 求值结果是布尔类型
    2>=1, 2==2, 2!=2(不等于)
     <,>,<=
#   logical operators逻辑运算
    && 与 and
    || 或 or
    ！ 非 not， 反转
    and，or，not 与或非
    对布尔类型进行推算
*   三元运算符 ternary/conditional opertator
*   条件运算符 e？a：b
*   ？：
*   console.log(3>5?1:2), 结果为2  
*   冒号左边为真，冒号右边为假
#   未定义的值
    null与undefined可以互换
#   自动类型转换
*   NaN，0，("")都为false， 0==false，nan==false
*   ===严格相等，！==不严格相等
*   a===b为T，a！==b为F
#   防御性编程
    建议使用===
#   逻辑运算符的短路特性：确定了结果了就不需要继续往后运算
    || 左边转为布尔为真，返回左边；左边转为布尔为假，返回右边；这个功能用于返回默认值
    &&功能与||相反；
    && 左边转为布尔为真，返回右边；左边转为布尔为假，返回左边；比如
    打仗&&发射导弹，如果打仗为T，则发射导弹，如果打仗为F，则不发射导弹
    truthy：转为布尔运算为真
    laslsy：转为布尔运算为假
#   作业

#  charpter 2 Program Structure 编程结构
#   Expreesions（表达式） and statements（结构）
    当每一栏的开头是 + - / （ [ `， 必须在每个句子结尾加上分号，其他情况可以省略。
    可以写成 (简称分号前置)
    ；+
    ；-
    ；/
    ；(
    ;[
    ;`
    if语句{}后面可以不加分号
    变量语句{}后面要加分号
    写分号的规则比不写分号的规则复杂，大部分情况下不写
# Variables 变量
    var caught（变量名）（不能以数字开头） = （不是等号的意思，=为赋值）5 * 5；变量声明语句及赋值语句
    apple = apple + 1； = 意思为赋值，先计算右边，然后赋值给左边
    math.sqrt（） 根号

#   关键字和保留字 keywords and reserved words
#   运行环境 The environment
#   Functions 函数
    consloe.log( "foo" + "bar", 222);
#   Return values 返回值
    console.log(Math.max(2, 4)));
    函数的求值结果
    function calls 函数调用
    f（x）=2x 可以写成
    var f = (x) => 2 * x
#   prompt and confirm 弹窗和确认  
    var shouldGo = confirm('should we go?')
    var age =prompt('你几岁了？')
    console.log('你是' + (2021 - age) + '出生的')   
    x.toFixed(2)
        保留数值x小数点后两位 
    Number() 
        将字符串变成数字
    Math.sqrt( )
        开平方；
    .toFixed(0) 
        取整，四舍五入；
    .toFixed(x)
        保留小数点后面X个数；
    *Math.floor()
        向下取整；
    *Math.ceil()
        向上取整；
    *Math.trunc()
        删除掉数字的小数部分和小数点，不四舍五入；
    *Math.round()
        返回小数点最近的整数  四舍五入；
    *console.log('foo %s bar, 3333) 输出*foo3333bar， 
        %s 占位符 
    *String()
        把数字转换成字符串
    *Math.abc()
        返回绝对值
#   control flow 控制流 

#   Conditinal execution执行条件
    * isNaN  is not a number
        判断是否是数字
    * Number.isNaN(NaN)
        判断是否是NaN
    *if(xxx))
        {一条语句；{}把多条语句合成一条语句
    }else if(yyy)——————————else的子语句
        {一条语句；
    }else
        一条语句；}
    
